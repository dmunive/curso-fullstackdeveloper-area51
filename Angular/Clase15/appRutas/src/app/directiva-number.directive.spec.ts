import { DirectivaNumberDirective } from './directiva-number.directive';

describe('DirectivaNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivaNumberDirective();
    expect(directive).toBeTruthy();
  });
});
