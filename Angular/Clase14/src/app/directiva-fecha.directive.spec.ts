import { DirectivaFechaDirective } from './directiva-fecha.directive';

describe('DirectivaFechaDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivaFechaDirective();
    expect(directive).toBeTruthy();
  });
});
