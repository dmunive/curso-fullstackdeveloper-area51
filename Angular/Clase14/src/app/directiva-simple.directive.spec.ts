import { DirectivaSimpleDirective } from './directiva-simple.directive';

describe('DirectivaSimpleDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivaSimpleDirective();
    expect(directive).toBeTruthy();
  });
});
