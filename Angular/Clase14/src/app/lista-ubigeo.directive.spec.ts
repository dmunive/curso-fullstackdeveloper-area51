import { ListaUbigeoDirective } from './lista-ubigeo.directive';

describe('ListaUbigeoDirective', () => {
  it('should create an instance', () => {
    const directive = new ListaUbigeoDirective();
    expect(directive).toBeTruthy();
  });
});
