import { DirectivaInputDirective } from './directiva-input.directive';

describe('DirectivaInputDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivaInputDirective();
    expect(directive).toBeTruthy();
  });
});
