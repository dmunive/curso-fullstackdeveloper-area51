export class Servidor {
    /*nombre: string
    proveedor: string
    ip: string
    usuario: string
    contrasena: string
    sistema: string
    ftp: string*/

    constructor(public nombre: string, public proveedor: string, public ip: string, public usuario: string, public contrasena: string, public sistema: string, public ftp: string){
        /*this.nombre = nombre
        this.proveedor = proveedor
        this.ip = ip
        this.usuario = usuario
        this.contrasena = contrasena
        this.sistema = sistema
        this.ftp = ftp*/
    }
}
